# GarageLog iOS V2.1

Bug fix:
- Vehicle switching now works correctly.
- The maintenance interval button no longer duplicates every time the dashboard re-renders.
- Service worker cache bumped to V2.1.

Replace the existing files in the GitHub Pages repository.
